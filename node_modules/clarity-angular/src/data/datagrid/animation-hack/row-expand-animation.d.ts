import { ElementRef, Renderer2 } from "@angular/core";
import { DomAdapter } from "../render/dom-adapter";
import { RowExpand } from "../providers/row-expand";
import { DatagridRenderOrganizer } from "../render/render-organizer";
export declare class DatagridRowExpandAnimation {
    private el;
    private domAdapter;
    private renderer;
    private expand;
    private renderOrganizer;
    constructor(el: ElementRef, domAdapter: DomAdapter, renderer: Renderer2, expand: RowExpand, renderOrganizer: DatagridRenderOrganizer);
    private running;
    private oldHeight;
    private animate();
    private run();
}

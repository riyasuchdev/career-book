import { TemplateRef, ViewContainerRef, OnInit, OnDestroy } from "@angular/core";
import { RowExpand } from "./providers/row-expand";
/**
 * TODO: make this a reusable directive outside of Datagrid, like [clrLoading].
 */
export declare class DatagridIfExpanded implements OnInit, OnDestroy {
    private template;
    private container;
    private expand;
    constructor(template: TemplateRef<any>, container: ViewContainerRef, expand: RowExpand);
    private updateView();
    ngOnInit(): void;
    ngOnDestroy(): void;
}

import { ElementRef, EventEmitter } from "@angular/core";
import { Point, PopoverOptions } from "../common/popover";
export declare class Dropdown {
    elementRef: ElementRef;
    parent: Dropdown;
    _open: boolean;
    _openChanged: EventEmitter<boolean>;
    isMenuClosable: boolean;
    private _menuPosition;
    anchorPoint: Point;
    popoverPoint: Point;
    anchor: any;
    popoverOptions: PopoverOptions;
    constructor(elementRef: ElementRef, parent: Dropdown);
    readonly isRootLevelDropdown: boolean;
    menuPosition: string;
    toggleDropdown(): void;
    open: boolean;
    onMouseClick(target: any): void;
}

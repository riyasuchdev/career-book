import { EventEmitter, TemplateRef, ViewContainerRef } from "@angular/core";
import { PopoverOptions, Point } from "./popover";
export declare class PopoverDirective {
    private templateRef;
    private viewContainer;
    private _popoverInstance;
    private _subscription;
    anchorElem: any;
    anchorPoint: Point;
    popoverPoint: Point;
    popoverOptions: PopoverOptions;
    clrPopoverChange: EventEmitter<boolean>;
    constructor(templateRef: TemplateRef<any>, viewContainer: ViewContainerRef);
    clrPopover: boolean;
    createPopover(): void;
    destroyPopover(): void;
    ngOnDestroy(): void;
}

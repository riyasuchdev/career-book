import { Observable } from "rxjs/Observable";
import { LoadingListener } from "../../../utils/loading/loading-listener";
export declare class RowExpand implements LoadingListener {
    expandable: boolean;
    replace: boolean;
    private _loading;
    loading: boolean;
    private _expanded;
    expanded: boolean;
    private _animate;
    readonly animate: Observable<boolean>;
    private _expandChange;
    readonly expandChange: Observable<boolean>;
    startLoading(): void;
    doneLoading(): void;
}
